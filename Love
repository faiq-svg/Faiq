<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Love, Maddoo ❤️</title>
    <style>
        body {
            background-color: #ffdde1;
            text-align: center;
            font-family: 'Arial', sans-serif;
            color: #d63384;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 50px auto;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
            animation: fadeIn 2s ease-in-out;
        }
        h1 {
            color: #e63946;
        }
        .heart {
            color: red;
            font-size: 50px;
            animation: heartbeat 1.5s infinite;
        }
        p {
            font-size: 18px;
            line-height: 1.6;
        }
        .btn {
            background-color: #e63946;
            color: white;
            padding: 10px 20px;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 20px;
        }
        .btn:hover {
            background-color: #c71d32;
        }
        .hidden-message {
            display: none;
            margin-top: 20px;
            font-size: 20px;
            color: #e63946;
        }
        @keyframes heartbeat {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>For My Love, Maddoo ❤️</h1>
        <p>To my dearest Maddoo,</p>
        <p>From the moment you came into my life, everything changed in the most beautiful way. You are my happiness, my love, my forever.</p>
        <p>Every smile of yours melts my heart, and every moment with you is a treasure I hold close.</p>
        <p>I love you more than words can say. Always and forever. 💖</p>
        <div class="heart">❤️</div>
        <button class="btn" onclick="showMessage()">Click for a surprise</button>
        <p class="hidden-message" id="hiddenMessage">You are the love of my life, Maddoo! 😘</p>
    </div>

    <script>
        function showMessage() {
            document.getElementById("hiddenMessage").style.display = "block";
        }
    </script>
</body>
</html>
